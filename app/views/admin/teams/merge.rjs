page.remove "team_#{@team_to_merge.id}_row"
page.replace_html "team_#{@existing_team.id}_aliases", render(:partial => 'aliases', :locals => { :team => @existing_team })
page.hide('warn')
page.replace_html('notice', "Merged #{@merged_team_name} into #{@existing_team.name}")
page.show('notice')
page.call("YAHOO.util.DragDropMgr.unlock")
page.call(:restripeTable)
page << "$('team_#{@existing_team.id}').removeClassName('disabled');"
page.visual_effect(:opacity, "team_#{@existing_team.id}_row", :from => 0.5, :to => 1.0)
